const subjectJava = Object.values(
//   subject.reduce((acc, cur, i) => ({
//     ...acc,
//     [i]: [...cur].flatMap((_, j, arr) =>
//       new RegExp(`java`, "i").test(arr.slice(j).join``) ? cur : []
//     )[0] || acc[i]
//   }), {})
// ).filter(Boolean);