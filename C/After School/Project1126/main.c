#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main() {
	//int n, *p;
	//scanf("%d", &n);
	//p = (int*)malloc(sizeof(int) * n);
	//p[0] = 5;
	//printf("%d", p[0]);

	//int n, * p;
	//printf("개수 입력:");
	//scanf("%d", &n);
	//p = (int*)malloc(sizeof(int) * n);
	//for (int i = 0; i < n; i++) {
	//	scanf("%d", &p[i]);
	//	p[i] += p[i];
	//}
	//printf("%d", p);
	
	//int a, * p;
	//printf("개수 입력:");
	//scanf("%d", &a);
	//p = (int*)malloc(sizeof(int) * a);
	//for (int i = 0; i < a; i++) {
	//	scanf("%d", &p[i]);
	//	*p += p[i];
	//}
	//printf("%d", *p / a);

	//int a = 0, *p =NULL;
	//int i = 0;
	//while (1) {
	//	p = (int*)realloc(p, sizeof(int) * (a + 1));
	//	scanf("%d", &p[a]);
	//	if (p[0] == 0) break;
	//	a++;
	//}
	//sub(p);

	//char n = '\0', * p = NULL;
	//int a;
	//while (1) {
	//	p = (int*)realloc(p, sizeof(int) * (n + 1));
	//	getch("%s", &p[n]);
	//	if (p[0] == EOF) break;
	//	n++;
	//	if (p[n] == 'a') {
	//		a = p[n];
	//	}
	//}
	//printf("%d 개", &a);

	return 0;
}