#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
	// 함수 정의 -> 리턴형 함수이름(매개변수){ 내용 }
	
	// 하나의 정수를 입력 받고 4제곱한 결과를 반환하는 함수
	
	// 실수하나를 입력 받고 입력 받은 값을 함수로 전달하여 소수점 첫째자리에서 반올림
	
	//main에서 실수 2개 입력 받고 두 실수의 정수 부분의 합을 반환하는 함수
	//두 실수의 합을 반환하는 함수는 실수의 정수부분만 반환하는 함수를 호출

	//문자를 입력받고 문자를 함수에 전달하면 대문자인지 소문자인지 반환하는 함수
	//main에서 대소 문자 출력

//int mult(int n) {
//	int s = 1;
//	for (int i = 0; i < 4; i++) {
//		s = s*n;
//	}
//	return s;
//}

//int sosu(double n) {
//	int c = n;
//	if (n - c >= 0.5) {
//		c += 1;
//	}
//	return c;
//}

//int sum(double);
//int twososu(double, double);
//int sum(double new) {
//	return (int)new;
//}
//int twososu(double n, double e) {
//	return sum(n) + sum(e);
//}

int chartrcen() {

}

int main() {
	//int mult(int);
	//int a = 0;
	//int b;
	//printf("입력:");
	//scanf("%d", &a);
	//int n = a;
	//b = mult(n);
	//printf("%d의 4제곱의 결과는 %d 입니다.",a ,b);
	
	//int sosu(double);
	//double a = 0;
	//int b;
	//printf("입력:");
	//scanf("%lf", &a);
	//double n = a;
	//b = sosu(n);
	//printf("%lf 의 결과는 %d",a , b);

	//double a;
	//double b;
	//int d;
	//printf("입력:");
	//scanf("%lf %lf", &a , &b);
	//d = twososu(a,b);
	//printf("%d", d);

	
	return 0;
}