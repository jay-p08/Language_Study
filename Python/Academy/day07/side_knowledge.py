if __name__ == "__main__" :
    # in 키워드
    # in 키워드 좌측에 있는 값이 우측에 있는
    # iterable 객체의 요소로 존재하는지 여부르르 반환하는 키워드
    # 문자열의 경우, 이터러블 객체로 취급하기 때문에
    # in 키워드를 사용할 수 있다.
    # -> 주의 : 대소문자를 구분한여 비교한다.
    print("H" in "hello world")